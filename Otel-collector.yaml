mode: daemonset

image:
  repository: otel/opentelemetry-collector-contrib
# PRESETS: This handles the volume mounts for logs and all RBAC permissions automatically
presets:
  logsCollection:
    enabled: true
    includeCollectorLogs: false
  kubernetesAttributes:
    enabled: true
  kubeletMetrics:
    enabled: true
  hostMetrics:
    enabled: true
# FIX: This forces the creation of a ClusterIP Service so we can find the endpoint
service:
  enabled: true
  type: ClusterIP
serviceAccount:
  create: true
  name: otel-collector

# FIX 1 & 2: Added explicit RBAC rules for GKE and K8s metadata
clusterRole:
  create: true
  rules:
    - apiGroups: [""]
      resources: ["pods", "nodes", "nodes/stats", "nodes/proxy", "namespaces"]
      verbs: ["get", "list", "watch"]
    - apiGroups: ["apps"]
      resources: ["replicasets", "deployments", "daemonsets", "statefulsets"]
      verbs: ["get", "list", "watch"]
    - apiGroups: ["batch"]
      resources: ["jobs", "cronjobs"]
      verbs: ["get", "list", "watch"]

extraEnvs:
  - name: K8S_NODE_NAME
    valueFrom:
      fieldRef:
        fieldPath: spec.nodeName
  - name: NEW_RELIC_LICENSE_KEY
    value: "4664e57b5e7e853b63baf195c3ef88b2FFFFNRAL"
  - name: K8S_CLUSTER_NAME
    value: "k8s-cluster"

config:
  receivers:
# Adding OTLP now so the ports open, even if no one is using them yet
    otlp:
      protocols:
        grpc:
          endpoint: 0.0.0.0:4317
        http:
          endpoint: 0.0.0.0:4318
    kubeletstats:
      collection_interval: 60s
      auth_type: serviceAccount
      endpoint: "https://${env:K8S_NODE_NAME}:10250"
      insecure_skip_verify: true
    hostmetrics:
      collection_interval: 60s
      scrapers:
        cpu: {}
        memory: {}
        disk: {}
        network: {}

  processors:
    batch: {}
    memory_limiter:
      check_interval: 1s
      limit_mib: 400
    k8sattributes:
      auth_type: serviceAccount
    resource:
      attributes:
        - key: k8s.cluster.name
          value: ${env:K8S_CLUSTER_NAME}
          action: upsert

  exporters:
    debug:
      verbosity: normal # This prints spans to the collector logs
    otlphttp/newrelic:
      # FIX 3: Removed the '/v1/metrics' suffix to prevent 404
      endpoint: "https://otlp.nr-data.net" 
      headers:
        api-key: ${env:NEW_RELIC_LICENSE_KEY}

  service:
    pipelines:
      metrics:
        receivers: [kubeletstats, hostmetrics, otlp]
        processors: [memory_limiter, k8sattributes, resource, batch]
        exporters: [otlphttp/newrelic]
      logs:
        # Receiver is added automatically by preset (filelog)
        processors: [memory_limiter, k8sattributes, resource, batch]
        exporters: [otlphttp/newrelic]
      traces:
        receivers: [otlp]
        processors: [memory_limiter, k8sattributes, resource, batch]
        exporters: [otlphttp/newrelic, debug] # Add 'debug' here
